<view class="page"><view class="heading">全部模块</view><view class="subtitle">每一种工作和生活场景都有自己的记录。</view><navigator wx:for="{{modules}}" wx:key="title" class="panel" url="{{item.route}}" open-type="{{item.tab ? 'switchTab' : 'navigate'}}">{{item.title}} →</navigator><navigator class="panel" url="/pages/memos/index">快速备忘 →</navigator></view>
