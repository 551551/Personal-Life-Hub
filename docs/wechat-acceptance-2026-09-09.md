# 页面验收与提审交接（2026-09-09）

对象：开发版本 1.2.3，AppID `wx3e05a9b25530c50a`。本报告不是微信审核通过证明。

## 用户真机反馈

用户本日明确反馈“备份真机测试已通过”，此项从待测试更新为用户确认通过；此前记录重开保留也已由用户确认。未提供机型、系统和全部页面交互结果，因此不扩大验收范围。

## 已完成的页面契约检查

新增 `node --test scripts/wechat-page-acceptance.node-test.mjs`：21 项通过，无跳过。使用编译后的原生运行代码和隔离内存存储，不读写用户数据。

| 页面/场景 | 本次已检查 | 尚未确认 |
|---|---|---|
| 首页、今日 | 生命周期完成、非忙碌、无错误、首页模块路由 | 实际控件点击、滚动、改期交互 |
| 科研 | 空数据初始化、文献/实验/论文入口 | 项目与里程碑界面及手机键盘 |
| 训练 | 训练表单默认类型、动作子入口 | 选择父计划及长表单操作 |
| 更多 | 八模块路由与 tab 跳转方式 | 模拟器连续点击 |
| 备忘、自媒体 | 空数据初始化、配置及模板存在 | 编辑/取消/删除真实控件 |
| 通用记录 | 全部 11 类表单字段、选项索引、子入口可达 | 所有表单手机布局和键盘遮挡 |
| 设置 | 初始化不报错且不偷偷写入存储 | 本日用户确认备份真机测试通过；其他控件待完整验收 |

全部 9 个注册页面的静态链接以及首页动态模块链接已核对注册路径和 tab 模式。11 类业务表单均可从静态入口、模块入口或子入口到达。已有事件绑定及官方 WXML 编译检查仍是独立测试，不能用本测试替代。

新测试首轮首页/今日两项失败：测试没有等待异步 `onShow` 完成，提前断言 `busy=false`。核对实现后，让测试等待真实生命周期 Promise；保留原断言，再运行 21 项全部通过。此为测试夹具修正，**不等于修复原生导航超时**。本次没有修改业务运行代码，也不需要覆盖已上传的 1.2.3。

## 原生渲染与业务流程复验

`node scripts/wechat-page-render-smoke.cjs` 本日 20:27 完成：**19 个页面/表单场景全部通过**，包括 8 个独立页面及通用记录页的 11 类表单。每个场景均等待真实导航回调、检查当前路径/加载状态/错误、通过 `wx.createSelectorQuery` 核对实际渲染节点尺寸，滚动并保存顶部与底部截图。产物：`release/wechat-page-render/result.json` 和 38 张 PNG。已逐模块查看代表截图，未发现首尾内容不可达、白屏或关键表单消失；这不是所有手机键盘、弹窗、每个点击及所有数据状态的验收。

- `wechat-main-modules-smoke.cjs`：11 类表单原生保存、八模块入口、今日完成回写、设置初始化通过；仅删除本次生成的测试记录。
- `wechat-stale-edit-smoke.cjs`：通用记录、备忘、自媒体、科研项目及里程碑的失效编辑保护均通过，未创建记录。
- `wechat-media-handler-smoke.cjs`：保存、发布阶段、筛选、清理自身记录通过。原固定等待 800 毫秒先复现 `Wrong page`，改为等待导航成功回调，原断言保留。
- `wechat-research-handler-smoke.cjs`：项目创建、里程碑完成、级联清理自身测试记录通过。原脚本用 `navigateTo` 进入 tab 页先复现 `Wrong research route`，改成 `switchTab` 并等待回调，原断言保留。
- 全仓库 Vitest 再次运行：35 文件、127 项通过。新增测试脚本语法检查与 oxlint 入口通过。

## 自动化超时证据与限制

- 官方工具版本 Stable 2.01.2510260；原项目基础库 3.17.2；自动化 SDK 0.12.1。
- 早期连接成功后，原项目 `evaluate` 可读取首页路径，但失效编辑回归脚本仍得到 `navigateTo:fail timeout`；后续复验已通过，不能抹掉早期失败历史。
- 截图与控件查询曾同样无响应，故问题不仅是某一个跳转 URL 拼写。静态路由检查没有发现未注册页面或 tab 模式错配。
- 官方 CLI 的 `--disable-gpu` 试验未恢复自动化；随后恢复普通启动，没有修改工具安装文件，也没有清理用户存储。
- 独立两页诊断工程也出现连接后应用调用不响应；因该工程未取得正常首屏基线，不把它作为“业务程序没有问题”的证明。
- 临时以 `--disable-backgrounding-occluded-windows --disable-renderer-backgrounding --disable-background-timer-throttling` 启动工具后，连续跳转和截图恢复。未修改工具包、系统配置或用户存储；这是本机有效的诊断启动方式，不证明其中某一个参数是唯一根因，也不是业务代码修复。
- 仍有独立问题：`wechat-page-ui-smoke.cjs` 明确报 `Protocol timeout: Page.getData`；原 `wechat-media-smoke.cjs` 控件测试仍超时。相同会话的 App 逻辑调用、截图及微信官方节点查询正常；定位到 SDK Page 协议通道，而非所有页面无法渲染。官方 npm 包本日最新仍为 0.12.1，没有盲目引入第三方替代依赖。
- 第三方维护者也公开报告相近环境中的 `Page.*` 整族无响应，作为旁证而不是本项目通过证明：[维护者原始实测](https://github.com/Zi-Yi-Ming/miniprogram-auto-test)。其替代点击实际上调用处理器，不能当作物理控件点击通过。因此本次将渲染检查、处理器流程、真实输入测试明确分开。

遵循调试和上线技能：保留失败，不提高超时后直接报通过。原生渲染及主流程已有真实证据，但当前仍不应宣称完整控件 E2E 或平台上线验收已完成。

## 微信后台提审材料（尚未提交）

官方说明：上传后的代码在“版本管理 → 开发版本”；管理员提交审核，审核结果在审核版本区域展示。审核通过后仍需手动发布。见 [微信官方接入流程](https://developers.weixin.qq.com/miniprogram/introduction/)（本日读取）。本机官方 CLI 的命令列表没有提交代码审核命令；上传成功并不意味着已经提交审核。

目前没有可操作的已登录微信公众平台后台会话，也没有当前备案/隐私等审核前置项的完整证据。尝试启动独立审核浏览器时，浏览器进程启动失败；独立启动命令随后被环境策略明确拒绝，未绕过该限制。需要管理员打开 [微信公众平台](https://mp.weixin.qq.com/)，提供版本管理及提审前检查页面的截图；不要提供账号密码、AppSecret、验证码、身份证或私人备份。

### 可供管理员核对的审核说明

> 本小程序是个人备忘和计划记录工具。支持今日计划、自媒体创作安排、科研学习、训练计划、饮食记录及兴趣活动记录。无需注册登录，不提供游戏、影视播放、资讯发布、医疗诊断、付费交易或社交服务。业务记录保存在当前设备；用户可以主动导出 JSON 备份至自己选择的微信会话，并选择文件恢复。没有自动云同步。审核可直接创建测试记录体验，无需测试账号。

上述为代码功能说明，不代替后台隐私指引或法律声明。类目“工具 > 备忘录”曾由用户截图显示已通过；是否覆盖当前全部功能，应如实展示并以平台审核意见为准，不隐瞒模块规避审核。

建议审核路径（按后台实际表单填写，不凭空补材料）：

1. `pages/home/index`：首页进入今日计划和各模块。
2. `pages/memos/index`：新增备忘 → 保存 → 编辑 → 取消/删除。
3. `pages/research/index`：先创建项目，再添加里程碑、文献或实验记录。
4. `pages/fitness/index`：新增训练计划，再进入训练动作。
5. `pages/settings/index`：阅读本地数据说明；备份恢复仅用审核测试数据。

最终提审前还需管理员核对名称/头像/简介、服务类目、备案状态、隐私指引及后台其他实际要求。凡涉及实名资料、联系方式、法律声明或管理员扫码，由管理员本人完成。只有取得真实审核结果后才能标记“审核通过”；目前未提交、未发布。
