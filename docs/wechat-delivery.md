# 微信小程序交付说明

交付日期：2026-09-09。开发版本：**1.2.3**。AppID：`wx3e05a9b25530c50a`。

## 当前结论

八个主模块已实现，1.2.3 经官方 CLI 上传成功（214265 字节，209.2 KB）。本次交付为**开发测试版本**，不是已审核上线的正式版。未完成的端到端检查没有被跳过或记为通过。

## 文件与使用方式

- 原生工程：`D:\软件\personal-life-hub\release\wechat`。
- 干净工程包：`D:\软件\personal-life-hub\release\wechat-1.2.3.zip`。解压后用微信开发者工具导入包含 `project.config.json` 的目录，可直接编译；它是编译后的原生工程，不是完整 TypeScript 源码包。
- 源码：`D:\软件\personal-life-hub\wechat`，共享模型在 `src/db/schema.ts`，共享备份结构在 `src/services/backup/backupSchema.ts`；需要修改或重建时使用整个本地项目。
- 构建和测试入口：`node scripts/verify-wechat.mjs`；全仓库测试：`node node_modules/vitest/vitest.mjs run`。
- 手机使用：管理员在微信公众平台核对开发版本 1.2.3，设置体验版后扫码。体验版权限和二维码由平台管理，不用下载上述 ZIP 到手机。

工程包来自已通过隔离产物检查的构建目录，不包含私人数据、备份、AppSecret、node_modules 或 `project.private.config.json`。发布目录 runtime 与隔离产物 SHA256 相同：`0BA4E24B4BFE9879292B88273A2A21453E37F7C840AF1AB503FAF6327A2CC515`。

ZIP 内 44 个文件已逐一与隔离产物校验一致。ZIP SHA256：`3BBDD0AB7713D53F66D642A627F9E431600322DAC67A47A0C3FCDCD96B4E7C12`。

## 功能清单

| 模块 | 当前主功能 |
|---|---|
| 首页 | 今日摘要、业务数量、快速备忘入口、模块导航 |
| 今日计划 | 临时任务、九类来源自动汇总、完成与改期回写、跨日刷新 |
| 自媒体 | 内容、平台、制作阶段、发布计划、实际发布日期、指标、素材与复盘 |
| 科研 | 项目、里程碑、文献笔记、实验全过程、论文、章节及修改项 |
| 训练 | 训练日期、动作、组数、次数/时长、重量、顺序 |
| 饮食 | 餐次、食物、数量单位、营养值、每日合计 |
| 娱乐与吉他 | 兴趣项目、进度与日期、曲目、练习时长与重点 |
| 数据与设置 | 17 表 JSON 备份、校验恢复、取消保护、清空二次确认 |

各模块使用本机存储，不自动跨手机、网页或桌面同步。换设备需要手动导出、恢复。不要把微信缓存或本地导出文件当作唯一长期备份。首次真机完整验收前，仅使用测试数据或已有独立副本的数据。

## 本轮修复与证据

1.2.1 修复跨日刷新与设置页旧错误提示；1.2.2 修复通用表单失效编辑入口；1.2.3 将保护补到备忘、自媒体、科研项目及里程碑，避免目标已删除时误写前一条记录。里程碑编辑重新读取当前存储，不信任旧列表副本。新问题均有修复前失败、修复后通过的回归测试。

- 全仓库：35 文件、**127 项测试通过**。
- 小程序：17 文件、**86 项测试通过**。
- 本次补充编译后页面契约检查 **21 项通过**：9 个注册页面初始化、11 类业务表单及导航入口检查，不计为实际控件 E2E。详见 `docs/wechat-acceptance-2026-09-09.md`；单独运行 `node --test scripts/wechat-page-acceptance.node-test.mjs`。
- 原生渲染检查 **19 个场景通过、38 张截图已保存**；主模块原生保存、今日回写、失效编辑保护、自媒体与科研处理器复验通过。真实控件 SDK `Page.getData` 仍超时，不能把这些证据算作全部控件 E2E。
- 17 张表均填充合成数据，导出、恢复、重开后完整相等，覆盖长文本、数值与父子关系。
- 表单字段覆盖与 schema 对照通过；18000 字长笔记只改标题后仍完整保留。
- 小程序与原项目 TypeScript、oxlint 入口通过；隔离构建的 5 项产物/官方 WXML 检查及发布目录官方模板编译通过。
- 2026-09-09 生产依赖审计 0 漏洞。原 Android 开发依赖中的一项中风险问题详见 1.2.0 报告，本次未升级或改动 Android。
- 用户已确认手机记录退出重开后保留。机型、系统及具体退出时机未提供，因此不推定为所有设备持久化验收通过。
- 2026-09-09 用户进一步确认备份真机测试通过：此项按用户反馈验收，机型、系统和测试录像未提供，不扩大为所有设备或全部页面均通过。

## 尚未通过的上线门槛

| 检查项 | 状态 |
|---|---|
| 用户手机记录重开保留 | 用户确认通过 |
| 手机导出、聊天选文件、恢复并核对数据 | 2026-09-09 用户确认备份真机测试通过 |
| 所有模块手机控件、键盘和导航完整检查 | 未完整确认 |
| 自动化连续页面跳转与原生渲染 | 19 个场景通过；截图可读取 |
| 官方 SDK 原生控件测试 | `Page.getData` 超时；不能宣称全部 E2E 通过 |
| 基础信息、备案、隐私指引、类目适配 | 未获得管理员后台完整确认 |
| 平台代码审核与正式发布 | 未提交、未发布 |

自动化排查区分出两类问题：临时关闭工具后台渲染节流后，连续跳转、截图及 `wechat-stale-edit-smoke.cjs` 已复验通过；官方 SDK `Page.*` 通道仍有独立超时，失败保留在 `wechat-page-ui-smoke.cjs` 及原 `wechat-media-smoke.cjs`。完整排查、38 张截图位置与各项验证边界见 `docs/wechat-acceptance-2026-09-09.md`。不能据此断言手机必然报同样错误，也不能把处理器调用冒充真实控件操作。

## 管理员待办（一次完成，不用清空真实数据）

1. 将 1.2.3 设为体验版，先确认页面显示正常。
2. 用测试记录逐一进入八个模块，验证新增、编辑、取消删除、确认删除和返回。科研/训练/吉他先建父记录，再建关联内容。
3. 今日页完成、改期后，回到来源模块检查状态和日期。
4. 备份真机测试本日已由用户确认通过，无需为推进本轮验收重复恢复真实数据。后续版本复测时仅使用测试备份；恢复会整体替换当前数据，重要数据必须先单独备份。
5. 将遇到的报错截图和操作步骤交给开发者；不发送账号密码、AppSecret、身份证、验证码或私人完整备份。
6. 上述验收完成后，再核对后台基础信息、隐私/备案/类目要求并提交审核；收到平台通过结果后再正式发布。

产品简介参考文案：用于个人备忘、任务安排、科研学习、训练及饮食与兴趣记录的本地管理工具。娱乐模块只记录个人计划和进度，不提供游戏或视频内容。是否符合所选类目以及需要哪些材料，以管理员后台当前要求为准。

## 维护与回退

本版不变更存储命名空间或备份 schemaVersion。发现严重问题应暂停提审/发布，保留数据并修复后重新测试。旧工程包 1.2.0/1.2.1 已保留，可按需重新上传开发版；回退不应清空微信存储。不要以仅支持备忘的更早版本覆盖多模块数据后继续写入。

完整交付不等于自动代办实名信息或保证微信审核通过。上述未完成门槛需要真实测试结果或管理员操作后，才能关闭最终上线验收。
