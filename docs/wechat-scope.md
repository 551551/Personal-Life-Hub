# 微信小程序适配范围（用户已确认）

## 已确认信息

- 项目：个人工作生活中心。
- 用户提供的 AppID：`wx3e05a9b25530c50a`。尚未通过开发者工具验证访问权限。
- 主体：用户按个人主体注册。
- 用户截图显示服务类目“工具 > 备忘录”已通过；这不是代码审核或上线通过。
- 现有应用采用 React 页面、HashRouter 路由、Dexie/IndexedDB 数据存储；当前未发现小程序工程。
- 保留现有网站、Windows 和 Android 版本，以及未提交的 Android 构建与文档修改。

## 拟定范围与假设

目标是在微信内提供原有个人计划和记录能力，交付可导入开发者工具、可真机预览的小程序工程，再配合用户完成审核发布。

建议采用独立的小程序页面和平台存储适配，不以网页地址嵌入作为交付方案。具体 API、基础库兼容范围及容量限制在实施前核对微信官方文档和开发者工具。

第一版默认保留八个模块。业务规则尽量复用，页面、导航、文件导入导出和存储能力按平台适配；不直接移用 React DOM、IndexedDB 或 Capacitor 原生接口。

数据默认仅保存在当前设备的小程序本地存储；不新增账号系统、服务器、云数据库、自动跨设备同步或数据分析上报。不能把本地存储视为永久备份，需提供容量或保存失败反馈，并说明数据清理风险。

## 能力划分（已确认）

| 模块 ID | 职责 | 依赖 |
| --- | --- | --- |
| mini-foundation | 工程配置、导航、平台接口隔离、本地存储与输入校验 | 无 |
| mini-records | 自媒体、科研、健身、饮食、娱乐各场景的记录与操作 | mini-foundation |
| mini-overview | 今日计划聚合、首页摘要、快速备忘，保持来源与完成状态一致 | mini-foundation、mini-records |
| mini-data | 数据与设置、兼容原有 JSON 备份、校验后导入、备份导出 | mini-foundation、mini-records |
| mini-release | 工程检查、真机验收、隐私说明、提交材料与上线操作说明 | 以上模块 |

实施顺序：基础工程 → 业务模块 → 首页与今日聚合 → 数据迁移 → 发布验收。按用户确认后的边界再写各模块规格和可执行任务。

## 第一版验收边界

- AppID 配置准确；管理员能在微信开发者工具中打开工程并真机预览。
- 八个模块可进入，各业务场景保留相应的数据和操作，不以通用待办列表替代所有模块。
- 新增、修改、完成、删除及跨页面汇总符合原有业务规则。
- 重启后能读取已成功保存的数据；保存失败不误报成功，删除需确认。
- JSON 备份导入导出经过往返测试；无效或超限备份不覆盖现有数据。
- 自动化测试、开发者工具编译与手机实际操作分别记录结果；模拟接口测试不能冒充真机测试。
- 现有网站和 APK 不受改造影响；不上传用户备份、密钥或本地业务数据。

## 需要用户操作或核实

- 用户确认上述功能范围、数据独立与不增加云同步的方案。
- 管理员自行完成开发者工具登录、扫码和身份验证；开发不需要提供 AppSecret。
- 备案、隐私声明、名称和服务类目等要求以后台当前要求为准，不能仅凭类目通过判断已满足上线条件。
- 上传、提交审核和正式发布分别确认状态；最终审核结果由微信平台决定。

## 当前状态

2026-09-05：用户回复“是”，确认以上范围。模块规格见同目录 `SPEC-mini-*.md`；技术计划与执行清单见 `../tasks/plan.md` 和 `../tasks/todo.md`。规格和计划待审阅，尚未创建小程序运行工程、安装开发工具、上传代码或提交审核。
