# 微信小程序 1.2.1 更新记录

官方 CLI 已返回 `√ upload`：开发版本 **1.2.1** 上传成功，包大小 **212876 字节（207.9 KB）**。这不是提交审核或正式发布；体验版选择需在管理员后台确认。

## 本轮用户确认

用户在体验版操作说明后回复“确认保留成功”。记录为：**用户真机记录退出重开后保留验证通过**。机型、系统版本及是否保存后立即退出未提供，不把此结果推定为所有机型、全部业务场景或备份恢复已通过。

## 开发内容

- 修复首页/今日计划在后台放置过夜后仍停留在旧日期的问题。跟随今日时重新进入页面会刷新本地日期及计划；用户主动选择的历史或未来日期继续保留；重新选择今天后恢复跟随。
- 修复设置页一次读取出错、再次成功读取后仍显示旧错误的问题。
- 新增备份完整恢复及数量刷新、坏文件拒绝、第二次清空确认取消时保留数据的测试。
- 数据结构、存储命名空间和备份格式不变；没有新增联网、权限采集、依赖或云存储。未修改原 Android/网页实现。

## 验证证据

- 两项跨日测试先失败，实施修复后通过；设置错误恢复测试先失败、修复后通过。
- `node scripts/verify-wechat.mjs`：15 个文件、77 项小程序测试通过；类型检查、隔离构建、5 项产物/官方模板检查、发布目录模板检查通过。
- 全仓库 Vitest：33 文件、118 项通过；原项目 TypeScript 和 oxlint 入口通过。
- `node scripts/wechat-date-smoke.cjs`：官方模拟器原生处理器验证日期跟随与主动日期保留通过；仅修改页面临时状态，恢复后退出，不修改业务记录。
- 首次模拟器运行发现旧 runtime 未刷新；完全退出并重新启动开发者工具加载新产物后通过，未忽略版本检查。
- 生产依赖审计 0 漏洞。原 Android 开发工具链的中风险依赖问题及已有原生控件自动点击超时记录仍见 1.2.0 报告，未宣称解决。

## 下一项真机验收

把更新的开发版本设置为体验版。下一步仅使用测试记录验证“数据与设置”的备份：导出到自己的文件传输助手，再从聊天文件选择该 JSON 恢复，核对记录；取消恢复应保持原数据。不需要清空真实数据来证明恢复功能。

仍待验证：其他模块全部真机交互、文件分享/选择/恢复、后台资料及审核。没有提交审核或正式发布。

## 回退

保留原 `release/wechat-1.2.0.zip` 工程包。必要时可重新上传旧开发版；本轮不更改数据模型，回退也不应清空缓存。提交审核前应先保留独立备份。
