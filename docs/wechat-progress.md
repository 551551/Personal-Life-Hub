# 微信小程序实施进度

## 2026-09-07：科研项目与里程碑增量

- 新增项目名称、说明、阶段、开始/截止日期、状态和进度；项目列表及里程碑选择、编辑、日期、优先级、完成状态。
- 项目删除展示关联记录总数并确认，原子删除该项目的里程碑、文献、实验、论文与章节；故障测试证明提交失败时保留父子数据，不影响其他项目。
- 新增 11 项测试；全仓库 85 项通过，独立小程序 44 项通过，类型检查及 5 项产物/官方 WXML 检查通过。
- 官方微信模拟器中通过页面处理器验证项目新增、里程碑新增和完成、删除本次测试项目及其里程碑。测试记录已删除。可重跑 `node scripts/wechat-research-handler-smoke.cjs`；仍不代表真机或原生控件点击验收。
- 原生 Page/Element 自动化接口超时仍未解决；App 运行时接口可用，未隐藏此前失败的控件测试。
- 文献、实验、论文/章节的独立页面，以及健身、饮食、娱乐、今日/首页、数据备份与上线流程仍待开发。

## 2026-09-07：自媒体模块实施增量

- 新增独立自媒体原生页面：标题、类型、多个平台、五个制作阶段、计划/实际发布日期、素材、四类指标和复盘；支持编辑、确认删除、筛选和分批加载。备忘页提供临时入口，完整首页/更多导航尚未实现。
- 新增 MediaService，复用现有完整 schema 与 SnapshotStore；微信入口共享同一个快照实例，防止不同模块各持旧快照导致写入冲突。不改变网页数据库、Android 或已发布站点。
- 素材/复盘列表最多预览 400 字，编辑从仓库读取全文，测试确认不会将截断摘要写回覆盖原文。
- 自动验证：新增 12 项测试（7 项业务、5 项页面），全仓库 74 项；独立小程序 33 项，5 项产物/官方 WXML 检查和类型检查通过。
- 微信模拟器通过媒体页面处理器保存、制作阶段筛选、仅本次测试记录删除；该验证不是实际点击验收。`scripts/wechat-media-handler-smoke.cjs` 保留可复用命令。
- 真实原生控件测试 `scripts/wechat-media-smoke.cjs` 在自动化 Page/Element 操作处超时，未删除或跳过该测试，未标记 UI 验收通过。需要继续排查此工具接口或人工验证页面；后续模块和正式发布均未完成。

## 2026-09-07：动态编译错误修复并完成模拟器闭环

- 获用户授权开启本机服务端口；实际连接官方自动化接口，读取到初始化调用栈进入 Zod `Doc.compile`。Zod 在 schema 创建时捕获 jitless，原设置晚于共享 schema 初始化；微信的 Function 构造器返回不可调用对象，探测误通过而运行失败。
- 增加先于共享 schema 执行的 `configureValidation.ts`，保留全部校验，不修改网页入口。新增产物回归模拟 Function 返回对象：修复前失败，修复后初始化与保存通过，动态编译探测次数为零。
- 自动检查：62 项 Vitest、21 项独立小程序测试（含于 62 项）、5 项产物/官方 WXML 检查、小程序类型检查通过。
- 实际微信开发者工具 2.01.2510260、基础库 3.17.2：初始化无错误；新增测试备忘；完全退出工具并重新启动后仍能读取；编辑成功；使用模拟确认删除仅本次测试记录。测试记录已删除，不影响用户记录。删除弹窗本身的点击和手机真机验证尚待完成。
- 只清理了编译缓存，没有清理 storage、登录状态或业务快照。开发工具已保持打开；官方 CLI 在退出紧接重开时有端口竞争，等待退出完成后重试成功。
- 本机自动化 SDK 安装在 `D:/软件/.cache/wechat-automation`，不进入产品依赖。可复用 `scripts/wechat-simulator-smoke.cjs seed <唯一__wechat_smoke_标记>`，完全重启后运行 `verify`；可设置 `WECHAT_AUTOMATOR_PATH`、`WECHAT_WS`。只在测试工程执行，验收目前为处理器级模拟器验证，并非完整 UI 点击测试。
- 完整八模块、备份迁移和发布仍未完成。

## 2026-09-06：初始化异常诊断版

- 用户模拟器显示 `Function.prototype.apply was called on [object Object]`。本地 VM 保存/重开测试未复现，因此不宣称根因已定位或问题已修复。
- 原页面只显示捕获异常的 message，Console 无调用栈。已增加本地结构化日志 `wechat_memo_failure`，包含操作、事件编号和最多 12 个调用栈位置；不序列化异常对象、异常消息、草稿或存储数据，不联网。
- 新增初始化错误诊断、日志失败不影响页面报错、日志不包含异常文本的测试。小程序 21 项测试、类型检查、产物与官方 WXML 编译检查通过。
- 工程仍为 `release/wechat`。请重新点击“编译”，在 Console 中查找 `wechat_memo_failure`，提供该行以定位微信运行时的实际出错位置。不需要删除本地数据。

## 2026-09-06：修复备忘页面 WXML 编译错误

- 用户截图暴露 `wx:if="{{items.length &lt; total}}"` 无法编译。新增官方 WXML 编译器回归测试，修改前复现同一错误（unexpected `;`，退出码 2），改为 `{{total > items.length}}` 后通过。
- `release/wechat` 已重新生成，并通过已安装微信开发者工具自带的 `wcc.exe` 编译检查。此检查不等于模拟器交互或真机验证。
- 验证入口改为 `node scripts/verify-wechat.mjs`（或 `pnpm.cmd verify:wechat`）：18 项小程序测试、类型检查、隔离构建的 3 项产物检查和官方模板编译检查，成功后才更新 IDE 工程，并再次编译检查该工程。
- 隔离构建使用系统临时目录，避免开发者工具生成的私人配置混入交付检查；不删除现有 IDE 工程或其 `project.private.config.json`，源资产复制也排除私人配置。
- 官方编译器默认位置为 `D:/WechatDevTools/code/package.nw/node_modules/wcc-exec/wcc.exe`；其他电脑设置 `WECHAT_WCC_PATH` 指向自己的官方编译器。缺少编译器时测试失败，不静默跳过。
- 下一步：在已打开的开发者工具中点击“编译”，验证新增、重开、编辑及删除。尚未上传、审核或发布。

## 2026-09-06：基础备忘闭环已实现，等待开发者工具人工检查点

当前不是完整八模块版本，也尚未上传审核或正式上线。按计划先验证可靠存储与原生页面，再扩展业务模块。

### 已完成

- 独立工程 `wechat/`，AppID `wx3e05a9b25530c50a`；产物 `release/wechat/`。
- 原生备忘页面：新增、编辑、确认删除、分页加载；失败保留草稿，不提示虚假成功。
- 复用现有全部 17 张业务表的 schema；原计划误计为 18 已纠正，并使用完整表名断言防止漏表。
- 本地数据分块、双槽提交、校验和、提交前容量检查与写入队列；读取损坏数据时停止，不自动清空。
- 最多 3 MiB 业务快照是应用的保守保护值，不是微信官方容量上限；实际空间不足时仍会拒绝写入，旧快照保留。
- 未新增依赖、未修改网页业务数据库、未覆盖原有 Android 改动。
- 官方微信开发者工具 2.01.2510260 安装至 `D:\WechatDevTools`，已启动交互窗口。

### 官方来源与环境

- [工程结构](https://developers.weixin.qq.com/miniprogram/dev/framework/structure.html)
- [页面注册](https://developers.weixin.qq.com/miniprogram/dev/framework/app-service/page.html)
- [同步存储](https://developers.weixin.qq.com/miniprogram/dev/api/storage/wx.setStorageSync.html)：单 key 1 MB、总量 10 MB，存在用户删除或系统清理风险。
- [存储容量查询](https://developers.weixin.qq.com/miniprogram/dev/api/storage/wx.getStorageInfoSync.html)：容量单位 KB。
- [官方备用下载页](https://developers.weixin.qq.com/miniprogram/dev/devtools/download_backup.html)：页面列出的稳定版 2.01.2510260。

以上文档已通过官方 HTTPS 读取。安装包来自官方 servicewechat.com 重定向，Authenticode 状态 Valid，签名者 Tencent Technology (Shenzhen) Company Limited。SHA256：`62981F7C5B3B4BC61641832A555C494326DB6C95C193D9E1E0F1A266D99969D6`。

### 实际验证

- 独立小程序测试：18 项通过。
- 全仓库 Vitest：21 个测试文件、59 项测试通过（包括新增 18 项），没有跳过原有测试。
- 产物检查：3 项通过，其中包含无 DOM/Node 全局、禁止动态字符串编译的 VM 环境中保存与重开读取；它不是微信模拟器或手机实测。
- 小程序 TypeScript 检查、原项目 TypeScript 检查通过。
- 小程序构建成功，核心 runtime 约 96 KB；原网页构建和离线产物审计通过。
- 执行了仓库 oxlint 入口，退出码 0，但本环境没有输出汇总，不把它计作独立微信编译验证。

### 重新验证

在项目根目录运行 `pnpm.cmd verify:wechat`。如果当前 Codex 的 pnpm 包装器无法定位本地命令，可直接运行：

```powershell
Set-Location -LiteralPath 'D:\软件\personal-life-hub'
node scripts/verify-wechat.mjs
```

每个命令失败时先修复，不应继续把产物当作已验证版本。

### 当前人工检查点

开发者工具首次启动后需要管理员扫码登录。CLI 检查还提示工具的服务端口关闭，未自动开启。请在工具界面登录，然后导入 `D:\软件\personal-life-hub\release\wechat`，选择无云服务，确认 AppID 正确，点击编译。

如果需要代理通过 CLI 执行后续编译/预览，用户可在工具“设置 → 安全设置”开启服务端口；这允许本机程序控制开发者工具，不等于开放公网网站。也可以保持关闭，由用户在界面点击编译和预览。

请先创建一条测试备忘、退出重开、编辑，再取消一次删除与确认一次删除。把编译结果或错误截图提供给助手。仅测试数据，不在此基础版录入唯一副本的重要记录。

### 尚未完成

开发者工具模拟器交互验证、管理员权限验证、真机持久化检查，以及今日、自媒体、科研、健身、饮食、娱乐、设置/备份等后续任务均未完成；后续按 `tasks/todo.md` 继续。官方 WXML 编译器检查已通过，但未上传微信平台，未提交审核，没有线上小程序码。
# 最新状态（2026-09-08）

八个主模块及备份/今日汇总已经实现，版本 1.2.0 经官方 CLI 上传成功，尚非正式上线。完整验证结果与剩余阻塞见 [1.2.0 验收记录](wechat-release-1.2.0.md)。下文为早期分阶段历史，旧的“尚未迁移/服务端口未开启”等描述不代表当前状态。
