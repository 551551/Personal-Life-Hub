import { useState, type FormEvent } from 'react'
import type { LeisureItem } from '../../types/domain'
type Input = Omit<LeisureItem, 'id'|'createdAt'|'updatedAt'>
export function LeisureForm({ onSubmit }: { onSubmit(input: Input): Promise<unknown> }) { const [title, setTitle] = useState(''); const [category, setCategory] = useState('游戏'); const [date, setDate] = useState(''); const [note, setNote] = useState(''); async function submit(e: FormEvent) { e.preventDefault(); await onSubmit({ title, category, status: 'wishlist', plannedDate: date || undefined, priority: 'low', progress: 0, note: note || undefined }); setTitle(''); setDate(''); setNote('') }; return <form className="domain-form" onSubmit={submit}><label>名称<input aria-label="娱乐内容名称" onChange={(e) => setTitle(e.target.value)} required value={title} /></label><label>类别<input aria-label="娱乐类别" list="leisure-categories" onChange={(e) => setCategory(e.target.value)} required value={category} /><datalist id="leisure-categories"><option value="游戏">游戏</option><option value="视频">视频</option><option value="电影">电影</option><option value="纪录片">纪录片</option><option value="书籍">书籍</option></datalist></label><label>计划日期<input aria-label="娱乐计划日期" onChange={(e) => setDate(e.target.value)} type="date" value={date} /></label><label className="domain-form__wide">备注<textarea aria-label="娱乐备注" onChange={(e) => setNote(e.target.value)} rows={2} value={note} /></label><button className="primary-button" type="submit">加入清单</button></form> }
